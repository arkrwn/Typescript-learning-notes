let booleanResultAnd = true && false; // Logical AND returns TRUE only if both operands are TRUE
console.log("Logical AND Result " + booleanResultAnd);

let booleanResultOr = true || false; // Logical OR returns TRUE if either one or both operands are TRUE
console.log("Logical OR Result " + booleanResultOr);

let booleanNotTrue = !booleanResultAnd; // Logical NOT inverts the value of a single operand (TRUE becomes FALSE)
// Ternary Operator - It is
// a shorter way of writing an IF statement
let x = 10;
let y = (x > 5)
console.log(y)